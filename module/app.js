import _ from 'lodash';
import 'angular';
import 'angular-ui-router'
import 'angular-material';

angular.module('chat', ['ngMaterial', 'ui.router'])
    .config(function ($stateProvider) {

    }).run(function ($state) {
});